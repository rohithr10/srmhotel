import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const primary = Color(0xFF6C63FF);

  static const textBoxColor = Colors.white;
  static const secondaryTextColor = Color(0xFF6C63FF);
}
